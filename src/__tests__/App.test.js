import React from "react";
import { shallow } from "enzyme";

import App from "../App";

it("renders", () => {
  // const wrapper = shallow(<App />);
  // const title = "Postimerkit";
  // expect(wrapper.contains(title)).toEqual(true);
});
